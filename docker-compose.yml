version: '3.8'

services:
  frps:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frps
    restart: always
    network_mode: host
    volumes:
      - ./frps.toml:/etc/frp/frps.toml
